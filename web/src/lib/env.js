export function getEnv() {
  return {
    // pricing display values
    priceMonthly: 5,
    priceLifetime: 39,
    priceDayPass: 2.99,

    // limits (match your server defaults)
    freeMaxChars: 1500,
    freeMaxExports: 3,
    proMaxChars: 20000,
  };
}
